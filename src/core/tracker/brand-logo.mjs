// Rolester brand mark — used for the dashboard header logo and favicon.
// Referenced by path (relative to the generated workspace/tracker.html) rather
// than inlined, so the repo carries the real file instead of a base64 blob.
// Trade-off: tracker.html must stay next to ../assets/ to show the logo.
export const BRAND_LOGO_SRC = "../assets/logo.png";
